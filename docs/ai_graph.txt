[START]
   │
   ▼
[Planner] ──── (needs_tool == False) ────────────────────┐
   │                                                     │
   │ (needs_tool == True)                                │
   ▼                                                     ▼
[Tool Getter] ◄────────┐                            [Drafter]
   │                   │ (rejected & retries < MAX)      │
   ▼                   │                                 ▼
[Tool Reviewer] ───────┘                            [Reviewer]
   │                                                     │
   └───────── (approved OR retries >= MAX) ──────────────┤
                                                         │ (rejected & retries < MAX)
                                                         ▼
                                                      [Drafter] ──► ...
                                                         │
                                                         └─ (approved OR retries >= MAX) ──► [END]