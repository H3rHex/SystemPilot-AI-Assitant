[project]
name = "systempilot"
version = "0.1.0"
description = "An ambitious yet achievable local and cloud-powered AI desktop assistant built to interact natively with your operating system."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "prompt-toolkit>=3.0.52",
    "rich>=15.0.0",
]
